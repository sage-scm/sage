[package]
name = "sage-config"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true
dirs = "6.0"
hack-workspace = { version = "0.1", path = "../../hack-workspace" }
toml = "0.8"
